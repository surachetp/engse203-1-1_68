# 📊 SOLUTION — Task Manager CLI

**Student:** Surachet Pengcom (68543210080-6)
**Date:** 2026-01-31

---

## 1) ภาพรวมของงาน
Task Manager CLI เป็นโปรแกรมบรรทัดคำสั่งสำหรับจัดการรายการงาน (tasks) โดยเก็บข้อมูลเป็น JSON ในไฟล์ (ค่าเริ่มต้น: ./data/tasks.json) โจทย์สำคัญคือการจัดการอ่าน/เขียนไฟล์อย่างปลอดภัย, ฟังก์ชัน CRUD ของ task, และฟีเจอร์ export/import ที่สามารถ merge ข้อมูลได้

---

## 2) รายละเอียดการใช้งานที่พัฒนา (Implementation)

### modules/storage.js
- read(): ตรวจสอบการมีอยู่ของไฟล์ด้วย fs.access() หากไม่มีจะคืน []; อ่านไฟล์ด้วย fs.readFile() และแปลง JSON ด้วย JSON.parse() (จับข้อผิดพลาดและ log)
- write(data): สร้างโฟลเดอร์เป้าหมายด้วย fs.mkdir(..., { recursive: true }), เขียนไฟล์ด้วย fs.writeFile()` โดยใช้ JSON แบบ pretty (indent 2)
- exportTo(filename, data): เขียนไฟล์ปลายทาง (สร้างโฟลเดอร์อัตโนมัติ)
- importFrom(filename): อ่านไฟล์และแปลงเป็น array คืนค่ากลับ

### modules/taskManager.js
- loadTasks()/saveTasks(): โหลดและบันทึก tasks โดยเรียก storage
- addTask(title, priority): ตรวจสอบ priority (low/medium/high) กำหนด id แบบตัวเลข (nextId) และ createdAt
- listTasks(filter): รองรับ all, pending, completed และแสดงเป็นตาราง
- completeTask(id): ทำเครื่องหมาย completed = true และเก็บ completedAt
- updateTask(id, newTitle): แก้ title และกำหนด updatedAt`
- deleteTask(id): ลบ task ตาม id
- showStats(): แสดงสถิติจำนวนงานทั้งหมด, เสร็จแล้ว, คงค้าง, แยกตาม priority
- exportTasks(filename): ใช้ storage.exportTo()
- importTasks(filename): ใช้ storage.importFrom(), merge เข้ากับ tasks เดิม และกำหนด id ใหม่เมื่อชนกัน

---

## 3) การทดสอบ (Manual)

ผมได้ทดสอบฟีเจอร์หลักดังนี้ (ผลทดสอบจากการรันคำสั่งบนเครื่องพัฒนา):

- เพิ่ม task: ✅ (runnning node index.js add "Test task" high เพิ่ม task และเขียนลงไฟล์)
- แสดง tasks (list): ✅ (รัน node index.js list แสดงรายการเป็นตาราง)
- ทำเครื่องหมายเสร็จ (complete): ✅ (task ถูกตั้ง completed และมี completedAt)
- แก้ไข title (update): ✅
- ลบ task (delete): ✅
- สถิติ (stats): ✅ (จำนวนรวม / แยก priority)
- Export: ✅ (ส่งออกไฟล์ JSON ตัวอย่างไปยัง docs/demo-export.json)
- Import (merge): ✅ (ฟังก์ชันถูก implement; สามารถทดสอบการ merge ผ่าน npm run demo ซึ่งจะทำ export -> import)

> หมายเหตุ: การทดสอบแบบ fully automated (unit tests) ยังไม่ถูกเพิ่มใน submission นี้ — ผมพร้อมเขียนชุดทดสอบ (Jest) หากต้องการ

---

## 4) ตัวอย่างการรันเดโม (สั้น ๆ)

```bash
# รัน demo script (ไม่แก้ไขไฟล์หลัก)
npm run demo
# หรือ
bash demo.sh
```

demo script จะใช้ DATA_FILE=./data/demo-tasks.json และทำลำดับการทำงาน: add, list, complete, update, delete, stats, export, import

---

## 5) ข้อสังเกต / ข้อจำกัด
- DATA_FILE ถูกอ่านจาก .env และพาธเป็น relative ต่อ current working directory — อาจทำให้สับสนหากรันจาก folder อื่น ควรพิจารณา resolve เป็น absolute path ตามตำแหน่งโค้ดหากต้องการความแน่นอน
- ไม่มีกลไก locking ขณะเขียนไฟล์ หากหลายกระบวนการเขียนพร้อมกันอาจเกิด race conditions

---

## 6) ไฟล์ที่แก้/เพิ่มสำคัญ (สำหรับผู้ตรวจ)
- Modified: modules/storage.js, modules/taskManager.js
- Added: demo.sh, docs/demo-export.json, SOLUTION.md (นี้), SUBMISSION.md, README.md

---

## 7) Packaging / Submission
สร้างไฟล์แพ็กสำหรับส่ง: level-2-challenge-submission.tgz (ตัด node_modules และ .git ออก)

คำสั่งแพ็ก (local):

```bash
tar --exclude='level-2-challenge/node_modules' --exclude='level-2-challenge/.git' -czf level-2-challenge-submission.tgz level-2-challenge
```

---

## 8) ต่อไป (ถ้าต้องการคะแนนเต็ม)
- เพิ่ม unit tests (Jest) สำหรับ storage และ taskManager พร้อมตัวอย่างผลการรัน
- เพิ่ม GitHub Actions เพื่อตั้งค่า CI (รัน tests) และ lint
- ปรับ DATA_FILE ให้ resolve เป็น absolute path เพื่อป้องกันปัญหา context

---

## 9) ติดต่อ
- ชื่อ: Surachet Pengcom
- รหัส: 68543210080-6

