# 📝 Task Manager CLI (level-2-challenge)

สรุปสั้น ๆ:
โปรเจคนี้เป็น CLI สำหรับจัดการ task ขนาดเล็ก เก็บข้อมูลเป็น JSON (ค่าเริ่มต้น: `./data/tasks.json`). มีคำสั่งพื้นฐาน: add, list, complete, delete, update, stats, export, import

---

## 🔧 การติดตั้ง

1. ติดตั้ง dependencies

```bash
npm install
```

2. ตั้งค่า (ไม่บังคับ)

สร้างไฟล์ `.env` และตั้งค่าตัวแปร (ค่าตั้งต้น):

```env
APP_NAME=Task Manager CLI
DATA_FILE=./data/tasks.json
LOG_LEVEL=info
```

---

## 🚀 การใช้งาน (ตัวอย่าง)

ให้รันคำสั่งจากโฟลเดอร์ `level-2-challenge`:

- เพิ่ม task

```bash
node index.js add "Buy groceries" high
```

- แสดงรายการ (กรอง: all/pending/completed)

```bash
node index.js list all
node index.js list pending
node index.js list completed
```

- ทำเครื่องหมายว่าจบแล้ว

```bash
node index.js complete 1
```

- ลบ task

```bash
node index.js delete 1
```

- แก้ไข title

```bash
node index.js update 1 "New title"
```

- แสดงสถิติ

```bash
node index.js stats
```

- ส่งออกไฟล์ JSON

```bash
node index.js export ./backup/my-tasks.json
```

- นำเข้าไฟล์ JSON (merge และจัดการ id ซ้ำอัตโนมัติ)

```bash
node index.js import ./backup/my-tasks.json
```

---

## 🧩 โครงสร้างสำคัญของโปรเจค

- `modules/storage.js`  - อ่าน/เขียน/ส่งออก/นำเข้าไฟล์ JSON
- `modules/taskManager.js` - ตรรกะการจัดการ tasks (add/list/complete/delete/update/stats/import/export)
- `index.js` - CLI entrypoint และ parsing คำสั่ง
- `data/tasks.json` - ที่เก็บข้อมูล tasks (JSON array)

---

## 🛠️ Troubleshooting & Tips

- หากคำสั่งไม่พบ `node` หรือขึ้น error ให้ตรวจสอบ Node.js เวอร์ชัน (แนะนำ Node v16+)
- ติดตั้ง dependencies ด้วย `npm install` ก่อนใช้งาน
- หาก export/import ไม่ทำงาน ให้ตรวจสอบพาธไฟล์และสิทธิ์การเขียน
- Demo: รัน `npm run demo` (หรือ `bash demo.sh`) — จะใช้ไฟล์ `data/demo-tasks.json` เพื่อไม่ไปแตะไฟล์หลัก

---

## ✨ ข้อเสนอแนะเพิ่มเติม (ถ้าต้องการคะแนนเต็ม)

- เพิ่ม unit tests (Jest) สำหรับ `modules/storage.js` และ `modules/taskManager.js`
- เพิ่ม CI (GitHub Actions) เพื่อรัน tests อัตโนมัติ
- ปรับ DATA_FILE ให้ resolve เป็น absolute path ตามตำแหน่งโปรเจค หากต้องการความแน่นอนขณะ deploy

---

## 📄 License & Contributing

- License: MIT (เปลี่ยนได้ตามต้องการ)
- หากต้องการให้ผมเพิ่ม `CONTRIBUTING.md` หรือ `LICENSE`, แจ้งชื่อ/ประเภทให้ผมจะเพิ่มให้ครับ

---

ต้องการให้ผมช่วยเพิ่ม unit tests / CI หรือต้องการผม push ขึ้น GitHub ให้หรือไม่? แจ้งผมได้เลย.